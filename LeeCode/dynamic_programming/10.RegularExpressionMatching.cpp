#include <iostream>
#include <vector>
#include <string>
using namespace std;

bool isMatch(string s, string p) {

}

int main (int argc, char* argv[])
{
    const string str1 = "aa";
    const string str2 = "a";

    bool ret = isMatch(str1, str2);
    std::cout << ret << std::endl;
    return 0;
}